import write_file from std.files

// Adds two numbers together
fn add :: a int -> b int -> int {
    return a + b
}

// Adds two numbers together
fn write_whatever {
    write_file "abc" "blah.txt"
}
